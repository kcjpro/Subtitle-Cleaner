# Optional: enables audio transcription with word-level timestamps.
# Don't install this in the same Python env you build the .exe from —
# PyInstaller's spec excludes it, but installing it just wastes disk space
# and slows the analysis pass.
#
# Install with:
#   pip install -r requirements-whisper.txt
faster-whisper>=1.0.0

# CUDA libraries for GPU acceleration on Windows. faster-whisper auto-falls
# back to CPU int8 if these aren't present.
nvidia-cublas-cu12; sys_platform == "win32"
nvidia-cudnn-cu12; sys_platform == "win32"
