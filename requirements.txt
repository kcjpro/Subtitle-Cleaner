# --- Required runtime deps ---
PySide6>=6.6.0

# Video playback. Requires libmpv-2.dll bundled in <app>/bin/ on Windows
# (or libmpv shipped via the OS package manager on Linux/macOS).
python-mpv>=1.0.5

# Scalable icon set (Font Awesome inside Qt) for toolbar / control buttons.
qtawesome>=1.3

# --- Optional ---
# See requirements-whisper.txt, requirements-llm.txt, requirements-visual.txt
# for the heavier optional dependencies.
