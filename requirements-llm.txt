# Optional: pluggable LLM backends for context-aware classification.
# Pick whichever you want — none are required if you'd rather use the local
# Ollama backend (which only needs HTTP, no SDK).
#
# Install with:
#   pip install -r requirements-llm.txt

# Google Gemini (free tier: gemini-2.0-flash, 15 RPM).
google-generativeai>=0.7.0

# Groq (free tier: llama-3.3-70b-versatile, ~30 RPM).
groq>=0.13.0
